<%= form_with model: @member do |form| %>
<%= form.collection_select :user_id, @users, :id, :username, include_blank: "None" %>
<%= form.collection_select :group_id, @groups, :id, :name, include_blank: "None" %>
<%= form.submit %>
<% end %>